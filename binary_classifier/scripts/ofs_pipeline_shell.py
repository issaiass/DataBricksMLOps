# Databricks notebook source
# Shell notebook so a Lakeflow pipeline can exist with a Jobs UI name.
# Online Feature Store sync is attached via existing_pipeline_id; do not put training here.
print("ofs-pipeline-shell")
