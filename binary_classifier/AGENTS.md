# Declarative Automation Bundles Project

This project uses Databricks Asset Bundles.

Install Databricks CLI >= 1.0. Use `databricks auth profiles` and pass `--profile` explicitly.

Read the repo `databricks-core` / `databricks-dabs` / `databricks-jobs` skills before changing jobs YAML.
